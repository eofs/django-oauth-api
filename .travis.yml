language: python

python:
    - "2.7"
    - "3.3"

env:
    - DJANGO="django==1.5.2"
    - DJANGO="https://www.djangoproject.com/download/1.6b2/tarball/"

install:
    - pip install $DJANGO
    - pip install djangorestframework>=2.3

script:
    - python runtests.py
