language: python

python:
    - "2.7"
    - "3.3"

env:
    - DJANGO="django==1.5.3"
    - DJANGO="https://www.djangoproject.com/download/1.6b3/tarball/"

install:
    - pip install $DJANGO
    - pip install djangorestframework>=2.3
    - pip install oauthlib==0.5.1

script:
    - python runtests.py
